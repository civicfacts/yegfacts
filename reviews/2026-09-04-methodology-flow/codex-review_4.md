# Review context: the methodology short version drawn as a flow, round 4

Repository root: the methodology-flow worktree of civicfacts/yegfacts
(branch `methodology-flow`, cut from `0c10681`). Round 3 left one
finding: outcome 4 said "a defect of fact" is corrected instead of
parked, which reads wider than the three narrow v1.20 defects, and put a
non-stopping path under "Stops here". The latest commit, `git log -1`,
answers it. Do not edit anything, do not build, do not run git commands
that touch the working tree. `dist/methodology.html` was built after
that commit.

## What changed since round 3

Step 2's second outcome now reads: "Stops here. After three reports, a
brief the check still calls unfair is parked until new evidence
arrives." It names only the framing case, which is the one that parks
(prompts/framing-check.md, v1.12). The v1.20 defect path is not claimed
here at all; the full method and the changelog carry it. No other text
changed; the layout is unchanged from round 3's screenshots.

## Review focus

Is the round-3 finding closed? Is outcome 4 true as worded? Anything
new. Concrete findings with file:line. No praise. End with REVISE or
APPROVED.
