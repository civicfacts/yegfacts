# Roads framing trace audit, 2026-09-09

Author: Stew, OpenAI GPT-6 Astra. Independent trace inspection by
Anthropic Opus 5 at high effort. The editor separately recomputed the
report matches, trace hashes and tool counts, and inspected the original
extraction log. This is an execution audit, not a fourth framing report.

## Corrections to the historical record

The claim that all three checks ran without web access is false. They made
23, 19 and 34 calls respectively to the plugin web tool. Total tool calls
were 25, 23 and 42. These count invocations, not individual URLs or successful
retrievals. The missing `--search` flag did not disable that tool.

The claim that report 3 invented the 669-comment count is also false.
The runtime supplied project memory outside the declared review package.
Report 3 read private extraction notes and cited them in its final response.
Its committed copy omitted that memory-citation footer. The substantive
report body is unchanged, but describing the entire response as verbatim
and unedited concealed the provenance of the disputed number.

Checks 1 and 2 match their original final responses after stripping the
added HTML headers and boundary whitespace. Check 3 matches only after also
removing the original memory-citation footer. The omitted footer identified
extraction session `01a06548-1c17-7362-a8e4-8044b2b8d801` and its private notes.
We preserve the committed reports as historical artifacts and record the
omission here rather than silently replacing them.

## What the capture evidence supports

The original extraction session visited the source URL in the capture
README. A browser snapshot displayed "669 comments". The later extraction
returned 621 records, including 286 replies. Its final check reported
"All comments", no visible reply-expansion buttons and no loading indicator.
The source URL, record count and first three records' identifiers, reply
links and displayed dates match the public capture. We did not compare every
archived record with the no-longer-present original export.

That supports a 48-count discrepancy between the earlier display and the
extraction. It does not identify 48 missing unique comments or establish
that Facebook exposed the complete thread. We have not verified report 3's
additional assertion that two stable end passes occurred. The public capture
README now states the supported limitation without adopting that assertion.
No captured record, pseudonym or account count changes.

The editor withdraws the fabrication allegation. The count did not come
from the declared package, but it had a source. The mistake was treating
absence from the package as proof of invention after the source citation
had been omitted. Private memory access also means a scratch working
directory did not isolate these checks from prior project context. The audit
found no direct read of a repository path in these sessions. That is an
observation about tool use, not proof that such reads were impossible.

## Source retrieval is a separate question

Report 3 attempted the linked 2024 FIR manual and received fetch failures.
It retrieved the 2018 edition and a reporting-changes bulletin before
making its objection. This was not an offline guess. It also does not make
the older manual authoritative for the brief's window. The trace audit
does not settle the financial-definition dispute or certify that every
mandatory source lookup succeeded.

## Current disposition

**PARKED, recorded 2026-09-09.** The v1.12 three-report cap, present in the
actual review package, says a third REVISE parks the brief. The prior editor
already conceded unresolved objections about report metadata, the program
name and the double-deduction guard. The capture limitation was real too.
Correcting the editor's allegations does not turn REVISE into FRAME OK.

The [independent procedural review](procedure-review-2026-09-09.md) rejected
continuing HELD while negotiating a new rule. The editor accepts that
recommendation and applies the existing cap. The review was conditional on
trace facts; the audit above verifies the facts needed for this disposition.
No cap reset, fourth report, fresh brief on the same intake or panel run
follows. Under the current rule, reopening requires new intake evidence,
not rediscovery of a limitation that already informed report 3.

Only the brief's editorial status annotation changes. The
[previous annotation](historical-status-2026-09-04.md) is preserved verbatim.
The substantive brief, intake supplied to the checker and all three
committed reports stay unchanged. The run record gains a dated correction,
and the shared capture README corrects its completeness claim. Triage is
unchanged: a parked framing run does not retroactively rewrite intake GO.

The procedural reviewer also proposed panel-ledger entries and broader
intake edits. This audit records the errors here without expanding the
panel-ledger's event or seat definitions or rewriting historical packages.
Runtime isolation and preservation of complete report provenance need
separate follow-through before another checker is described as isolated.
No methodology rule changes in this batch.

## Provenance and limits

The [trace manifest](trace-manifest-2026-09-09.json) records the three session
identifiers, full-file SHA-256 hashes, match rules and call counts. Original
traces remain private because they include machine context and, in the
extraction log, personal commenter identities. Hashes identify the inspected
files; they do not make those private files publicly inspectable.

Original extraction trace SHA-256:
`cd57ac15cc204df3ebb2609331d3998462e8ffe6b24f03916d817b010769e842`.

The pre-audit brief SHA-256 was
`43682c5aa48b9b4d48eb41a5749ed436ba51f7d8fedc36e2f8162a1a580b3c52`.
Its status annotation changes as disclosed above; this hash identifies the
historical file and does not claim a freeze or define a new parking rule.
No panel ran and this audit publishes no finding about who pays for roads.
