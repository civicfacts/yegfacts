# Public-exposure audit — 2026-09-02 (delta: active-transportation batch)

Delta record under `methodology/exposure-audit.md` for the files added on
the `active-transportation` branch (story four of the launch slate, the
Yegscoop capture and its candidates, evidence YF-EV-0114 to 0139). The
deterministic scan is what CI runs on every push; this record dispositions
the warnings that scan raises in the added files.

## 1. Deterministic scan

```
$ npm run audit:exposure
```

593 tracked text files scanned (16 binary skipped), 139 registry entries.

| Class | Severity | Count |
|---|---|---|
| SECRETS | fail | 0 |
| PRIVATE-EVIDENCE LEAK | fail | 0 |
| RIGHTS | fail | 0 |
| LOCAL PATHS | fail | 0 |
| PII | warn | 36 |
| LONG QUOTES | warn | 0 |

One LOCAL PATHS failure appeared on the branch's first CI run and was
removed before this record: the release-check auditor had quoted a
home-directory path from its own scratch directory inside its report, and
the report was committed with the quotation. The path never referred to a
tracked file.

## 2. Warn dispositions for the added files

**PII, "street address", 9 findings in this batch. ACCEPTED.**

- `reviews/active-transportation/2026-09-02-rerun/combined-evidence.json:43`,
  `round1/claude.json:231,244`, `round2/claude.json:276`, `synthesis.json:80`;
  `reviews/active-transportation/2026-09-02/combined-evidence.json:40`,
  `round1/claude.json:201,305`. Every hit is a capital-profile name or a
  route limit from the City's own budget documents and reports ("124
  Street - 109 Avenue to 118 Avenue", "50 Street CPR Grade Separation",
  "163 Street from 87 Avenue to 95 Avenue"). They name roads, not
  dwellings, and no person.
- `intake/candidates/lanes-removed-citywide/triage.md:86,87`: the
  triage reader quoting the captured thread's "102nd Ave between 121st
  and 111th" and "132nd ave". Street segments named by commenters as
  places where travel lanes changed; no residential address.

The pattern the scanner uses (a number followed by Street or Avenue) is
the right one for a city whose residential addresses look exactly like
that, so these stay as warnings and the disposition is recorded here
rather than by loosening the rule.

**PII, email address, 1 finding in `CLAUDE.md:10`. ACCEPTED.** The
organisation's own repository address in the session bootstrap, not a
person's.

## 3. Findings outside this batch

The 26 remaining warnings predate the branch: the corresponding-author
addresses in two public archives and the design-review transcripts
(dispositioned in `2026-09-01.md`), and the seven heritage-house
addresses in the low-density-history run files, which the board record
lists as needing their own disposition and which this batch does not
touch.

## 4. Capture-specific check

The Yegscoop capture (`intake/captures/2026-09-02-yegscoop-bike-lanes.md`)
was written with private commenters replaced by numbers before it was
committed; the raw thread with names is held only in the private board
repository. The release-check auditor (GPT) confirmed no commenter
identity leaks from the redacted capture. The one named private person
in the run files is a journalist's byline on a cited article, kept as
attribution.
